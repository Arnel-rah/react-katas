git
=> branch ==> (main, hotfixes, release branches, develop, feature branches)

=> Devoir => former groupe(via formulaire), revision react(pokedex => add formulaire creation(enregistrer en localstorage les pokemons créer)) => Afficher plus liste sur pokemon
